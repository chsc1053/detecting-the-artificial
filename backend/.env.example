# Server
PORT=3000

# Database
DATABASE_URL=postgres://localhost:5432/detecting_artificial_dev

# Admin “Stimuli upload” tab (S3 presigned PUT). On AWS, prefer an IAM role on the instance instead of keys.
AWS_REGION=us-east-1
AWS_S3_BUCKET=example-media-bucket
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=